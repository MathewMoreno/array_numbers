// create an empty array of numbers
let arrayLoop: number[] = []
// create a for loop that increments from 0 through 4
for (let index = 0; index < 4; index++) {
    arrayLoop[index] = index *2
    game.splash("This multiplies the index times two: " + arrayLoop[index])
}
// return the array loop that multiplies that number by 2

// create a second for loop that increments from o through 4

// display the number and the array number
